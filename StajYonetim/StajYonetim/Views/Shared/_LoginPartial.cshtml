

    @if (Request.IsAuthenticated)
    {
        @model StajYonetim.Models.UserProfile
     

        
            @Model.DisplayName

            <br />
            @Html.ActionLink("Cikis yap", "SignOut", "Account")
        
    }
    else
{   
<input type="button" class="Button" id="gonder" name="Gonder" value="Kullanıcı Girişi" onclick="location.href='@Url.Action("SignIn", "Account" )'" />
}
